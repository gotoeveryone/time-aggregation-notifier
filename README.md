# Time aggregation notifier

## Requirements

- Python 3.7
- pipenv

## Setup

```console
$ pipenv install # When with dev-package add `-d` option.
```

## Run (Local)

```console
$ pipenv run execute
```

## Deploy

```console
$ pipenv run deploy
```
